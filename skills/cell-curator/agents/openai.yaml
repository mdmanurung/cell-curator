interface:
  display_name: "Cell Curator"
  short_description: "Guided local-first hierarchical cell annotation"
  default_prompt: "Use $cell-curator to inspect and canonicalize AnnData or MuData, approve assumptions, run parent-scoped evidence cross-checks, reconcile critics, and prepare guarded write-back."
